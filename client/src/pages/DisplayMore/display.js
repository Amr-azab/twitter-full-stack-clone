import React from "react";
import { ModalDiv } from "../../component/div";
const Display = ({ showDisplayOptions }) => {
  return <ModalDiv onClick={showDisplayOptions}></ModalDiv>;
};

export default Display;
